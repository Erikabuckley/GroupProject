// Load the test module
const test = require('node:test');
// Use strict assertion mode for better error messages
const assert = require('node:assert/strict');

//login works
test('todo option with message', { todo: 'login works' }, (t) => {
  // valid and invalid
});

//test signup works
test('todo option with message', { todo: 'sign up works' }, (t) => {
  // valid and invalid
});