// Load the test module
const test = require('node:test');
// Use strict assertion mode for better error messages
const assert = require('node:assert/strict');

//test button to home screen works
test('todo option with message', { todo: 'test exeter logo goes to dash' }, (t) => {
  // This code is executed.
});

// test log out works
test('todo option with message', { todo: 'test log out goes to index' }, (t) => {
  // This code is executed.
});